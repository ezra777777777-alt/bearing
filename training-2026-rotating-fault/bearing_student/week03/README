
# Week 03: 编程基础与机器学习破冰

## 1. 本周目标
- [x] 完成 Python + PyTorch (GPU版) 环境配置。
- [x] 掌握张量 (Tensor) 的基本操作与广播机制。
- [x] 理解自动求导 (Autograd) 机制。
- [x] 手动实现线性回归的闭环训练。

## 2. 本周数据与任务范围
- 本周主要使用 `torch.randn` 生成模拟的传感器测量数据，验证模型训练流程，暂未接入真实的轴承振动数据集。

## 3. 本周完成的代码
- `test_gpu.py`：环境验证脚本。
- `tensor_basics.py`：张量变形与算术运算。
- `linear_regression.py`：单变量线性回归的从零实现。

## 4. 运行方式与配置
- 操作系统：Windows
- 显卡环境：NVIDIA GPU (CUDA 12.1)
- 运行指令：直接在虚拟环境中执行 `python code/linear_regression.py`

## 5. 关键结果
在线性回归实验中，经过 300 轮训练，Loss 下降至 `[0.0321]`，模型预测参数逼近真实规律。

## 6. 当前问题与风险
- 刚接触编程，对代码报错还不够敏感，有时会被维度不匹配 (Shape Mismatch) 卡住。
- [待补充你这周遇到的其他困惑]

## 7. 下周计划
- 深入阅读《统计学习方法》或相关论文，开始接触真实的轴承特征数据。








3月30日：完成情况：环境配置验证完成（GPU 可用），项目目录初始化。
        学习笔记：掌握了 reshape 和广播机制（理解了不同形状张量如何自动匹配）。

3月31日：1.掌握了tensor的切片规则，理清了torch.mm 与逐元素相乘的区别
        2.掌握张量连结（concatenate）语法，了解拼接规则与用途。

4月1日：学习了自动求导机制（Autograd),理解了requires_grad的作用，实验了梯度的清零与方向传播。

4月2日：理解了学习框架。手动实现了一个线性回归，理清了模型、损失、梯度更新的闭环过程

