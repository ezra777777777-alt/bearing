# Week 04：振动信号仿真与包络特征提取

## 1. 本周目标
- 掌握轴承局部故障的物理振动机理（低频冲击与高频共振）。
- 学习并使用 Python (NumPy/SciPy) 仿真带有真实物理意义的振动信号。
- 理解原始 FFT 分析的局限性，并掌握**包络解调（Hilbert变换）**技术以提取低频故障特征。
- 将手工提取的物理特征无缝对接到上周搭建的 PyTorch MLP 模型中，跑通前向传播。

## 2. 本周数据与任务范围
- **数据来源：** 纯代码数学仿真。
- **物理参数设置：** - 采样率 $f_s$ = 12000 Hz
  - 故障特征频率 $f_d$ = 50 Hz
  - 系统共振频率 $f_n$ = 3000 Hz
  - 阻尼系数 $\beta$ = 500
- **任务范围：** 生成 1 秒长的带噪一维时域信号，进行频域转换，并提取 1X(50Hz)、2X(100Hz)、3X(150Hz) 的能量特征。

## 3. 本周完成的代码 / Notebook
- `src/signal_simulation.py`: 核心信号仿真与分析脚本。包含了时间轴构建、冲击模拟、加噪、直接 FFT 以及包络谱特征提取。
- `src/mlp_test.py`: 极简全连接网络测试脚本。用于验证提取的 `[1, 3]` 维度特征张量能够成功送入网络并输出 Logits 分数。

## 4. 运行方式与配置
1. 确保已激活虚拟环境：`conda activate [你的虚拟环境名称]` 或 `.venv\Scripts\activate`。
2. 确保安装了 `numpy`, `matplotlib`, `scipy`, `torch`。
3. 在终端运行仿真脚本查看图表：
   ```bash
   python src/signal_simulation.py